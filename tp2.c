#include <stdio.h>
#include <stdlib.h>
#include "graph.h"

int main(int argc, char *argv[])
{
	int i, j, dimx, dimy;
	FILE *entrada;

	fscanf(entrada, "%d %d", &dimx, &dimy);

	for(i = 0; i < dimx; i++)
	{
		for(j = 0; j < dimy; j++)
		{
			/* code */
		}
	}

	return 0;
}
